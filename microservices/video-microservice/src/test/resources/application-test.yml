jpa:
  default:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        hbm2ddl:
          auto: create-drop
resource-urls:
  vm:
    videos: /videos
    users: /users
    health: /health